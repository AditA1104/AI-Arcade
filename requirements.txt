# Core dependencies for Pose-Controlled AI Arcade.
# Kept minimal on purpose: mediapipe/opencv pull in their own transitive
# deps automatically, and pinning unrelated packages (jax, scipy, etc.)
# on top caused unresolvable numpy version conflicts before.
#
# Only install opencv-contrib-python — never opencv-python alongside it,
# both provide the 'cv2' module and installing both breaks it.

pygame==2.6.1
opencv-contrib-python==4.11.0.86
mediapipe==0.10.21
numpy==1.26.4
